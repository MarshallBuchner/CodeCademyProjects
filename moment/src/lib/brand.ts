/** Public support inbox — swap when you create a dedicated address. */
export const SUPPORT_EMAIL = "marshallbuchner96@gmail.com";

export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}`;

export const SITE_NAME = "MOMENT";
